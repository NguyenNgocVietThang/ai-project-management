# ============================================================
# Core Framework
# ============================================================
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# ============================================================
# Database
# ============================================================
sqlalchemy==2.0.35
asyncpg==0.29.0          # Async PostgreSQL driver
alembic==1.13.3

# ============================================================
# Auth & Security
# ============================================================
# PyJWT replaces python-jose 3.3.0, which is affected by CVE-2024-33663
# (algorithm confusion) and CVE-2024-33664 (JWT bomb DoS) and is unmaintained.
PyJWT[crypto]==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1            # Last bcrypt release fully compatible with passlib 1.7.4
python-multipart==0.0.32  # For OAuth2 form data (>=0.0.18 fixes CVE-2024-53981 DoS)
email-validator==2.2.0    # Required by pydantic EmailStr (auth/user schemas)

# ============================================================
# Cache & Queue (Redis + Celery)
# ============================================================
redis==5.1.1
slowapi==0.1.10          # Redis-backed rate limiting for auth & abuse-prone endpoints
celery[redis]==5.4.0
flower==2.0.1             # Celery monitoring UI (optional)

# ============================================================
# AI Providers
# ============================================================
openai==1.51.0
google-generativeai==0.8.0

# ============================================================
# File Storage (MinIO / S3)
# ============================================================
minio==7.2.9
boto3==1.35.0             # Optional: AWS S3 compatible
Pillow==11.0.0            # Validate and normalize uploaded avatars

# ============================================================
# Email
# ============================================================
fastapi-mail==1.4.1
jinja2==3.1.4             # Email templates

# ============================================================
# Report Generation
# ============================================================
python-docx==1.1.2        # DOCX export
openpyxl==3.1.5           # XLSX export

# ============================================================
# HTTP Client (for internal calls)
# ============================================================
httpx==0.27.2

# ============================================================
# Utilities
# ============================================================
python-dateutil==2.9.0
pytz==2024.2
